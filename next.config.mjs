/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['placehold.co','lh3.googleusercontent.com','firebasestorage.googleapis.com'], // Add placehold.co to the allowed domains
    },
    
  };
  
  export default nextConfig;
  